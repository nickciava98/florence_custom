<odoo>
    <record id="account_move_form_inherit" model="ir.ui.view">
        <field name="name">account.move.form.inherit</field>
        <field name="model">account.move</field>
        <field name="inherit_id" ref="account.view_move_form"/>

        <field name="arch" type="xml">
            <xpath expr="//div[@name='journal_div']" position="after">
                <label for="is_vendor_bill" string="Vendor Bill" invisible="True"/>
                <div name="is_vendor_bill_div" class="d-flex" invisible="True">
                    <field name="is_vendor_bill"/>
                </div>

                <label for="is_manufacturing_bill" string="Manufacturing Bill"
                       attrs="{'invisible': [('is_vendor_bill','=',False)]}"/>
                <div name="is_manufacturing_bill_div" class="d-flex"
                     attrs="{'invisible': [('is_vendor_bill','=',False)]}">
                    <field name="is_manufacturing_bill"/>
                </div>
            </xpath>
        </field>
    </record>
</odoo>